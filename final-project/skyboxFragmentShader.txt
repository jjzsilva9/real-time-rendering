#version 330 core
out vec4 FragColor;

in vec3 TexCoords;

uniform samplerCube skybox;

void main()
{    
    vec3 skyColor = texture(skybox, TexCoords).rgb;
    
    FragColor = vec4(skyColor, 1.0);
}